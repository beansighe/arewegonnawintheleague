//use gonnawintheleague::LeagueTable;

//let NUM_SIMULATIONS: f32 = 1.0;

fn main() {

    // STEPS
    // read in target team
    // read in data
    //  data needed:
    //      - current table standings
    //          - team name
    //          - points
    //          - goals for
    //          - goals against
    //      - remaining fixture list
    //          - home team
    //          - away team
    // create threads, t = # of threads
    // run NUM_SIMULATIONS/t per thread
    // track how often team lands at given spot or above
    //      let mut success = 0.0;
    //      if run_simulation(queried_team) >= queried_rank {
    //          success += 1;
    //      }
    // store in some kind of mutex ???
    // does current league need to be a mutex? it shouldn't be modified
    // divide final count by NUM_SIMULATIONS
    //      success/NUM_SIMULATIONS
    // send result to web
}
